---
title: "CLI Installation"
description: "How to install the Setu Gateway CLI tool."
---

# CLI Installation

Install the Setu Gateway CLI globally via npm or homebrew:

```bash
npm install -g @setu/cli
```

Verify installation:

```bash
setu --version
```
