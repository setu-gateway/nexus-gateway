---
title: "CLI Installation"
description: "How to install the Nexus Gateway CLI tool."
---

# CLI Installation

Install the Nexus Gateway CLI globally via npm or homebrew:

```bash
npm install -g @setu/cli
```

Verify installation:

```bash
nexus --version
```
