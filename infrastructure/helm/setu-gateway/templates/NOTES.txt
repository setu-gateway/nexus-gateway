Setu Gateway has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Gateway service (in-cluster): {{ include "setu-gateway.gatewayName" . }}:{{ .Values.gateway.service.port }}
{{- if .Values.dashboard.enabled }}
Dashboard service (in-cluster): {{ include "setu-gateway.dashboardName" . }}:{{ .Values.dashboard.service.port }}
{{- end }}

{{- if .Values.ingress.enabled }}

Ingress is enabled - once DNS points {{ .Values.ingress.host }} at your ingress
controller, the dashboard will be reachable at:
  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}
{{- else }}

Ingress is disabled. Reach the gateway from inside the cluster, or port-forward it:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "setu-gateway.gatewayName" . }} 8000:{{ .Values.gateway.service.port }}
{{- end }}

{{- if not .Values.secrets.existingSecret }}

WARNING: secrets.existingSecret is not set, so this chart rendered a Secret from
secrets.data in your values file. Make sure that values file (with real DATABASE_URL/
REDIS_URL/JWT_SECRET/API keys) is never committed in plaintext - use
secrets.existingSecret with a secret you manage via your own pipeline
(External Secrets Operator, Sealed Secrets, etc.) for anything beyond a first try.
{{- end }}

Run database migrations before sending traffic (this chart does not run them for
you - see RELEASING.md):
  uv run alembic upgrade head
